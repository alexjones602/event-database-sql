SELECT COUNT(*)
FROM events;

SELECT AVG(ticket_price)
FROM events;

SELECT MAX(ticket_price)
FROM events;

SELECT MIN(ticket_price)
FROM events;

SELECT AVG(event_duration)
FROM events;

SELECT MAX(event_duration)
FROM events;

SELECT MIN(event_duration)
FROM events;

SELECT SUM(number_of_DJs)
FROM events;

SELECT AVG(ticket_price) AS average_ticket_price
FROM events;